import React from "react"

import SEO from "../layouts/seo"
import { Link } from "gatsby";

const NotFoundPage = () => (
  <div>
  ' We do not found your page :( go here => '
  <Link to='/'>Home</Link>
  </div>
)

export default NotFoundPage
